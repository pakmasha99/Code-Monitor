# RAG 시스템 구현 계획 요약
**날짜**: 2025-10-21
**상태**: ✅ 구현 가능 - 저장소 접근 확인됨

## 핵심 결론

**RAG 시스템 구현이 가능합니다!** 주요 발견사항:

### ✅ 저장소 접근 가능
- connectome 서버에서 GitHub SSH 접근 **작동 확인**
- `git@github.com:jcha9928/Code-Monitor.git` 클론 테스트 성공
- 15명의 사용자 GitHub URL 등록됨
- 다중 저장소 URL 지원 기능 방금 구현 완료 (제출당 최대 5개)

### 🟢 인프라 준비됨
- PostgreSQL 데이터베이스 작동 중
- FastAPI 백엔드 (async 지원)
- Celery 5.3.6 + Redis 5.0.1 **이미 설치됨**
- Next.js 15.5.6 프론트엔드

### ⚠️ 필요한 구성 요소
- Redis 서버 실행 필요 (패키지는 설치됨, 서버 미실행)
- Qdrant 벡터 데이터베이스 설치 필요
- LLM API 키 (Anthropic Claude + OpenAI) 설정 필요
- 코드 분석 파이프라인 구현 필요
- 데이터베이스 스키마 확장 필요

---

## 현재 상태 평가

### 사용자 저장소 현황

**등록된 사용자** (15명):
```
1. Jiook Cha → git@github.com:jcha9928/Code-Monitor.git (SSH ✅)
2. Choi Jubin → https://github.com/jubilant-choi
3. Janice_Min → https://github.com/mieuxmin
4. Yujin Yun → https://github.com/yinijooy
... (총 15명)
```

**문제점**:
- 대부분의 사용자가 프로필 URL만 등록 (저장소 URL 아님)
- 많은 저장소가 **비공개**일 가능성 높음
- 실제 저장소 URL 수집 필요
- 비공개 저장소 접근 권한 설정 필요

**접근 검증 결과**:
- ✅ SSH를 통한 GitHub 클론 작동
- ✅ GitPython 설치 및 작동
- ✅ 다중 저장소 URL 지원 구현됨
- ❓ 랩 멤버의 비공개 저장소 접근 권한 확인 필요

---

## 구현 단계 (총 3-4개월)

### 1단계: 인프라 설정 (1-2주)

#### 필수 설치:
```bash
# Redis 서버 설치
sudo apt install redis-server
sudo systemctl start redis-server

# Qdrant 벡터 DB (Docker)
docker pull qdrant/qdrant
docker run -p 6333:6333 qdrant/qdrant

# Python 패키지
pip install anthropic openai qdrant-client tree-sitter
```

#### 데이터베이스 확장:
- `code_analysis` 테이블 생성
- 코드 분석 결과 및 메타데이터 저장

### 2단계: LLM 통합 (2-3주)

#### API 설정:
- Anthropic API 키 (Claude 3.5 Sonnet)
- OpenAI API 키 (text-embedding-3-small)

#### 코드 분석 서비스:
- Tree-sitter로 코드 파싱 (함수/클래스 단위)
- Claude로 코드 분석 및 요약
- OpenAI로 임베딩 생성
- Qdrant에 벡터 저장

### 3단계: 백그라운드 작업 처리 (2주)

#### Celery 작업:
- 저장소 자동 동기화
- 코드 파일 분석
- 임베딩 생성
- 증분 업데이트

### 4단계: RAG 쿼리 시스템 (2-3주)

#### 쿼리 파이프라인:
1. 사용자 질문 → 벡터 검색
2. 유사 코드 검색 (상위 20개)
3. 재순위화 (관련성 + 품질 + 최신성)
4. Claude로 최종 응답 생성

#### 예시:
```
질문: "다른 사람들이 인증 어떻게 구현했는지 알려줘"
→ 벡터 검색으로 관련 코드 찾기
→ 상위 5개 코드 예시 추출
→ Claude가 비교 분석 및 권장사항 제공
```

### 5단계: 프론트엔드 UI (1-2주)

- RAG 검색 페이지 (`/rag`)
- 질문 입력 폼
- 답변 및 코드 예시 표시
- 대시보드 연동

### 6단계: 테스트 및 최적화 (1-2주)

---

## 비용 예상 (연간)

### OpenAI (임베딩):
- **초기**: ~$0.15
- **연간**: ~$1

### Claude (코드 분석):
- **초기**: ~$67.50 (전체 코드베이스)
- **연간**: ~$327 (지속적 업데이트)

### Qdrant (벡터 DB):
- **자체 호스팅**: 무료
- **클라우드**: $75/월 (~$900/년)

### 총 비용:
- **초기 설정**: ~$70
- **연간 운영**: ~$330 (자체 호스팅 시)
- **월간**: ~$27

---

## 즉시 수행할 작업 (우선순위)

### 이번 주:
1. [ ] connectome에 Redis 서버 설치 및 시작
2. [ ] Qdrant Docker 컨테이너 실행
3. [ ] Anthropic API 키 발급
4. [ ] OpenAI API 키 발급
5. [ ] 기본 임베딩 생성 테스트

### 1-2주차:
1. [ ] **랩 멤버들에게 실제 저장소 URL 수집**
2. [ ] 비공개 저장소 접근 권한 설정 (PAT 또는 SSH 키)
3. [ ] `code_analysis` 테이블 마이그레이션
4. [ ] 기본 CodeAnalyzer 클래스 구현
5. [ ] 1개 저장소로 수동 테스트

### 3-4주차:
1. [ ] Celery 작업 구현
2. [ ] 자동 저장소 동기화 설정
3. [ ] RAG 쿼리 핸들러 구축
4. [ ] API 엔드포인트 생성

### 5-6주차:
1. [ ] 프론트엔드 UI 구축
2. [ ] 통합 테스트
3. [ ] 성능 최적화
4. [ ] 프로덕션 배포

---

## MVP 빠른 시작 (4주)

자동화 없이 기본 기능부터 구현:

### Week 1: 수동 테스트
```python
# 간단한 스크립트로 테스트
1. 1개 저장소 클론
2. 1개 Python 파일 분석
3. Claude에게 분석 요청
4. Qdrant에 저장
5. 검색해보기
```

### Week 2: 기본 API
```python
# FastAPI 엔드포인트
1. POST /api/rag/query - RAG 검색
2. 간단한 프론트엔드 페이지
3. 실제 랩 멤버 코드로 테스트
```

### Week 3-4: 자동화
```python
# Celery 백그라운드 작업
1. 저장소 자동 동기화
2. 모든 랩 멤버로 확장
```

---

## 위험 요소 및 대응책

### 위험 1: 비공개 저장소 접근
- **위험**: 대부분의 랩 저장소가 비공개일 수 있음
- **대응**: GitHub PAT 또는 SSH 키 설정
- **대안**: MVP는 공개 저장소만으로 시작

### 위험 2: LLM API 비용
- **위험**: 많은 코드 파일로 인한 비용 증가
- **대응**:
  - 분석 캐싱 (변경되지 않은 파일 재분석 안 함)
  - 속도 제한
  - 예산 알림
  - 작은 배치로 테스트 시작

### 위험 3: 벡터 DB 성능
- **위험**: 많은 임베딩으로 인한 검색 지연
- **대응**:
  - Qdrant는 수백만 벡터 처리 가능
  - 인덱스 최적화
  - 필터로 검색 공간 축소

---

## 성공 지표

구현 후 측정:
- **채택률**: 주간 RAG 검색 사용 랩 멤버 비율
- **만족도**: 답변 품질에 대한 사용자 피드백
- **커버리지**: 분석되고 임베딩된 코드베이스 비율
- **성능**: 평균 쿼리 응답 시간 (목표: <3초)
- **비용**: 실제 API 지출 vs 예산

---

## 예상 사용 시나리오

### 시나리오 1: 인증 구현 방법 찾기
```
사용자: "다른 사람들이 JWT 인증 어떻게 구현했는지 알려줘"

RAG 시스템:
→ 벡터 검색으로 관련 코드 찾기
→ 김철수의 auth.py, 이영희의 middleware.js 등 발견
→ Claude가 각 접근방식 비교:
  "김철수님은 Flask-JWT-Extended로 구현했고,
   이영희님은 커스텀 미들웨어를 사용했습니다.
   두 방법 모두 장단점이 있는데..."
→ 코드 예시와 함께 권장사항 제공
```

### 시나리오 2: 특정 알고리즘 구현 찾기
```
사용자: "누가 Transformer 모델 구현한 거 있어?"

RAG 시스템:
→ 박민수의 transformer.py 발견
→ 코드 스니펫과 설명 제공
→ "멀티헤드 어텐션 메커니즘을 이렇게 구현했습니다..."
```

### 시나리오 3: 데이터 전처리 패턴
```
사용자: "fMRI 데이터 전처리 코드 예시 보여줘"

RAG 시스템:
→ 여러 랩 멤버의 전처리 코드 발견
→ 다양한 접근방식 비교
→ 각 방법의 장단점 설명
```

---

## 기술 스택 요약

### 백엔드:
- **FastAPI**: REST API
- **PostgreSQL**: 메타데이터 저장
- **Qdrant**: 벡터 데이터베이스
- **Celery + Redis**: 백그라운드 작업
- **GitPython**: 저장소 동기화

### AI/ML:
- **Claude 3.5 Sonnet**: 코드 분석
- **OpenAI text-embedding-3-small**: 임베딩 생성
- **Tree-sitter**: 코드 파싱

### 프론트엔드:
- **Next.js 15.5.6**: React 프레임워크
- **TailwindCSS**: 스타일링
- **shadcn/ui**: UI 컴포넌트

---

## 결론

✅ **구현 준비 완료**

RAG 시스템 구현이 현재 인프라로 가능합니다:

### 긍정적 요소:
- ✅ 저장소 접근 작동 (SSH 확인)
- ✅ Celery + Redis 이미 설치됨
- ✅ 명확한 아키텍처 (원본 설계 문서 존재)
- ✅ 합리적인 비용 (~$330/년)

### 권장 시작점:
**4주 MVP 버전**: Celery 자동화 전에 수동 저장소 동기화 및 기본 RAG 쿼리에 집중

### 다음 단계:
1. Redis 서버 설치 및 시작
2. Qdrant Docker 컨테이너 실행
3. API 키 발급 (Anthropic + OpenAI)
4. 1개 저장소로 POC 테스트
5. 랩 멤버들에게 저장소 URL 수집

### 예상 타임라인:
- **MVP**: 4주
- **전체 시스템**: 3-4개월
- **비용**: 월 $27

더 자세한 내용은 `RAG_IMPLEMENTATION_PLAN.md`를 참조하세요.
